export enum UserRole {
  ROOT = "root",
  ADMIN = "admin",
  CLIENT = "client",
  WORKER = "worker",
}


export enum CompanyStatus {
  PENDING_VERIFICATION = "pending_verification",
  VERIFID = "verified",
  ACTIVE = "active",
  INACTIVE = "inactive",
}

